apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lychee-ingress
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    kubernetes.io/ingress.class: nginx
    external-dns.alpha.kubernetes.io/hostname: img.jdani.eu
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
spec:
  tls:
    - hosts:
        - img.jdani.eu
      secretName: lychee-cert
  rules:
    - host: img.jdani.eu
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: lychee-service
                port:
                  name: http
